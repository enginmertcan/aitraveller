export interface TravelPlan {
    destination: string;
    duration: string;
    startDate: string;
    budget: string;
    groupType: string;
    numberOfPeople: string;
    itinerary?: any;
    userId: string;
    createdAt: Date;
  }